# Page for display table
helper.table:
  path: 'helper/table'
  defaults:
    _title: 'Month-Quarter-Year Table'
    _controller: 'Drupal\helper\Controller\HelperController::formTable'
  requirements:
    _permission: 'access content'
